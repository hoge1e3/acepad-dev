#!run

export async function main(){
let c=`sources
newcmd
npm-init
npm-test
npmfix
webpage
keysel
edit
acecmdls
diff
ls
findword
upload`;
    for(let k of c.split("\n")){
        this.cp("/jsmod/bin/"+k,"/show/bin/"+k);
    }
}