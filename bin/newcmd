//!run

export async function main(n){
    if(!n){
        throw new Error("cmd name");
    }
    let d=this.resolve(import.meta.url).sibling(n);
    if(!d.exists()){
        d.text(`#!run

export async function main(){

}`);
    }
    this.edit(d);
}