#!run

export async function main(n){
    if(!n){
        throw new Error("cmd name");
    }
    
    let d=this.resolve(this.$path.split(":").shift()).rel(n);
    //resolve(import.meta.url);
    if(!d.exists()){
        d.text(`#!run

export async function main(){

}`);
    }
    this.edit(d);
}