import {addPath, execFile} from "acepad-python";
import * as assert from "assert";
export async function main(){
    this.cd("test/");
    addPath(this.resolve("./"));
    const er=execFile(this.resolve("test.py"));
    console.log(er);
    let r=window.testResult;
    this.echo(r);
    assert.equal(r,42);
}    
