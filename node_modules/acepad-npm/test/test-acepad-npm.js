import * as npm from "acepad-npm";
import * as assert from "assert";
export async function main(){
    const pa=this.resolve(
        import.meta.url
    ).up().rel("../package.json");
    this.echo(pa.path());

    this.echo(...await npm.getDeps(pa));
}    
