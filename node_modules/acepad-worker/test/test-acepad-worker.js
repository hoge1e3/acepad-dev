import * as worker from "acepad-worker";
import * as assert from "assert";
export async function main(){
    const m=this.resolve("../acepad-worker-tester");
    this.echo(m);
    const w=await worker.create(m);
    const rpcu="https://unpkg.com/@hoge1e3/rpc@1.0.1/dist/index.js";
    const rpc=await import(rpcu);
    const c=await rpc.proxy.client(w,"test");
    this.echo(await c.test(21));
}    
