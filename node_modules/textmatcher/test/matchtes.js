import {matcher} from "textmatcher";
import * as assert from "assert";
export function main(){
    let r=matcher("abc def ").
    match(/\w+/,(m,w)=>({found:w}));
    assert.equal(r.found,"abc");
    this.echo(r.found);
}