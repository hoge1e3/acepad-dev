// textmatcher
export function matcher(str){
    const m={
        match(reg,f){
            let r=reg.exec(str);
            if(r){
                let a=Array.from(r);
                return f(matcher(rest,a));
            }else return m;
        }
    };
    return m;
}