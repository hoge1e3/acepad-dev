import { ModuleValue } from "../types";
export declare const reservedWords: {
    [key: string]: 1;
};
export declare function uniqueName(avoidThem: string[]): string;
export declare function valueToESCode(valueName: string, value: ModuleValue, properties?: string[]): string;
