import { SFile } from "@hoge1e3/sfile";
import { IAliases, IModuleCache, Module, ModuleValue, ScriptingContext } from "../types";
import { CompiledCJS, ModuleEntry } from "./Module.js";
type RequireFunc = ((path: string) => ModuleValue) & {
    deps: Map<string, Module>;
    freezeDeps(): void;
    resolve(path: string): string;
};
export declare class CJSCompiler {
    static create(aliases: IAliases): CJSCompiler;
    aliases: IAliases;
    get cache(): IModuleCache;
    get ctx(): ScriptingContext;
    constructor(aliases: IAliases);
    requireFunc(base: SFile): RequireFunc;
    requireArguments(file: SFile): [RequireFunc, ModuleValue, {
        exports: ModuleValue;
    }, string, string];
    compile(entry: ModuleEntry): CompiledCJS;
}
export declare function require(aliases: IAliases, path: string): ModuleValue;
export declare function require(aliases: IAliases, file: SFile): ModuleValue;
export declare function require(aliases: IAliases, path: string, base: SFile): ModuleValue;
export declare function require(aliases: IAliases, path: string, base: string): ModuleValue;
export {};
