// acepad-samples
export function copy(sh,reset=false){
    const dirn=sh.resolve(
        import.meta.url
    ).up();
    const dst=sh.resolve(sh.$home).rel("samples/");
    const src=dirn.rel("samples/");
    if(dst.exists()&&!reset)return;
    const bk=uniq(src,"samples");
    src.copyTo(dst);
}
function uniq(base,prefix){
    let res;
    do{
        
    }while(!res.exists());
    return res;
}
