export type IdleTimer = {
    postpone(): void;
    active: boolean;
};
export declare function idleTimer({ min, max, rate, handler, }: {
    min?: number;
    max?: number;
    rate?: number;
    handler: () => void;
}): IdleTimer;
