import { Buffer } from "buffer";
import RootFS from "./fs/RootFS.js";
export declare const path: typeof import("node:path");
import { BaseName, Canonical, Fstab } from "./types.js";
import { Dirent, FSTypeName, IFileSystem } from "./fs/types.js";
export declare const os: {
    platform: () => string;
    EOL: string;
};
export declare const process: {
    __fs: FileSystem | undefined;
    _cwd: string;
    env: {
        [key: string]: string;
    };
    argv: string[];
    argv0: string;
    execPath: string;
    execArgv: string[];
    pid: number;
    release: {
        name: string;
    };
    stdout: {
        write(...a: any[]): void;
        columns: number;
        fd: number;
    };
    stderr: {
        write(...a: any[]): void;
        columns: number;
        fd: number;
    };
    memoryUsage(): {
        heapUsed: number;
    };
    exit(): void;
    __setfs(fs: FileSystem): void;
    cwd(): string;
    chdir(path: string): void;
    nextTick<R = void>(fn: (...args: any[]) => R, ...args: any[]): Promise<R>;
    hrtime(a?: [number, number]): number[];
    versions: {};
    version: string;
    platform: string;
};
export declare const timeIncrements = 1000;
export type FdEntry = {
    buffer: Buffer;
    offset: number;
    close: () => void;
};
export type WatchFileListener = (old: Stats, current: Stats) => void;
/**
 * Represents a virtual POSIX-like file system.
 */
export declare class FileSystem {
    fdseq: number;
    fdEntries: Map<number, FdEntry>;
    private linkCache;
    promises: FileSystem_Promises;
    clearLinkCache(): void;
    getRootFS(): RootFS;
    constructor();
    /**
     * Gets a value indicating whether the file system is read-only.
     */
    _readOnly: boolean;
    get isReadonly(): boolean;
    /**
     * Makes the file system read-only.
     */
    makeReadonly(): this;
    /**
     * Recursively remove all files and directories underneath the provided path.
     */
    rimrafSync(_path: string): void;
    resolveLink(path: Canonical): [IFileSystem, Canonical];
    resolveLinkNoCache(path: Canonical): [IFileSystem, Canonical];
    resolveParentLink(path: Canonical): [IFileSystem, Canonical];
    /**
     * Make a directory and all of its parent paths (if they don't exist).
     */
    mkdirpSync(path: string): void;
    /**
     * Determines whether a path exists.
     */
    existsSync(_path: string): boolean;
    isMountPoint(path: Canonical): IFileSystem | undefined;
    childrenOfMountPoint(path: Canonical): IFileSystem[];
    /**
     * Get file status. If `path` is a symbolic link, it is dereferenced.
     *
     * @link http://pubs.opengroup.org/onlinepubs/9699919799/functions/stat.html
     *
     * NOTE: do not rename this method as it is intended to align with the same named export of the "fs" module.
     */
    statSync(path: string): Stats;
    /**
     * Change file access times
     *
     * NOTE: do not rename this method as it is intended to align with the same named export of the "fs" module.
     */
    utimesSync(path: string, atime: Date, mtime: Date): void;
    /**
     * Get file status. If `path` is a symbolic link, it is dereferenced.
     *
     * @link http://pubs.opengroup.org/onlinepubs/9699919799/functions/lstat.html
     *
     * NOTE: do not rename this method as it is intended to align with the same named export of the "fs" module.
     */
    lstatSync(__path: string): Stats;
    readlinkSync(__path: string): string;
    /**
     * Read a directory. If `path` is a symbolic link, it is dereferenced.
     *
     * @link http://pubs.opengroup.org/onlinepubs/9699919799/functions/readdir.html
     *
     * NOTE: do not rename this method as it is intended to align with the same named export of the "fs" module.
     */
    readdirSync(path: string): BaseName[];
    readdirSync(path: string, opt: {
        withFileTypes: true;
    }): Dirent[];
    /**
     * Make a directory.
     *
     * @link http://pubs.opengroup.org/onlinepubs/9699919799/functions/mkdir.html
     *
     * NOTE: do not rename this method as it is intended to align with the same named export of the "fs" module.
     */
    mkdirSync(_path: string, { recursive }?: {
        recursive: boolean;
    }): void;
    /**
     * Remove a directory.
     *
     * @link http://pubs.opengroup.org/onlinepubs/9699919799/functions/rmdir.html
     *
     * NOTE: do not rename this method as it is intended to align with the same named export of the "fs" module.
     */
    rmdirSync(_path: string, options?: {
        recursive?: boolean;
    }): void;
    rmSync(path: string, options?: {
        recursive?: boolean;
        force?: boolean;
    }): void;
    /**
     * Link one file to another file (also known as a "hard link").
     *
     * @link http://pubs.opengroup.org/onlinepubs/9699919799/functions/link.html
     *
     * NOTE: do not rename this method as it is intended to align with the same named export of the "fs" module.
     */
    linkSync(oldpath: string, newpath: string): void;
    /**
     * Remove a directory entry.
     *
     * @link http://pubs.opengroup.org/onlinepubs/9699919799/functions/unlink.html
     *
     * NOTE: do not rename this method as it is intended to align with the same named export of the "fs" module.
     */
    unlinkSync(path: string): void;
    /**
     * Rename a file.
     *
     * @link http://pubs.opengroup.org/onlinepubs/9699919799/functions/rename.html
     *
     * NOTE: do not rename this method as it is intended to align with the same named export of the "fs" module.
     */
    renameSync(src: string, dst: string): void;
    cpSync(_src: string, _dst: string, { recursive }?: {
        recursive: boolean;
    }): void;
    /**
     * Make a symbolic link.
     *
     * @link http://pubs.opengroup.org/onlinepubs/9699919799/functions/symlink.html
     *
     * NOTE: do not rename this method as it is intended to align with the same named export of the "fs" module.
     */
    symlinkSync(target: string, linkpath: string): void;
    /**
     * Resolve a pathname.
     *
     * @link http://pubs.opengroup.org/onlinepubs/9699919799/functions/realpath.html
     *
     * NOTE: do not rename this method as it is intended to align with the same named export of the "fs" module.
     */
    realpathSync(_path: string): Canonical;
    /**
     * Read from a file.
     *
     * NOTE: do not rename this method as it is intended to align with the same named export of the "fs" module.
     */
    readFileSync(path: string, encoding?: null): Buffer<ArrayBuffer>;
    /**
     * Read from a file.
     *
     * NOTE: do not rename this method as it is intended to align with the same named export of the "fs" module.
     */
    readFileSync(path: string, encoding: BufferEncoding): string;
    /**
     * Read from a file.
     *
     * NOTE: do not rename this method as it is intended to align with the same named export of the "fs" module.
     */
    readFileSync(path: string, encoding?: BufferEncoding | null): string | Buffer<ArrayBuffer>;
    private toPlainTextOrURL;
    /**
     * Write to a file.
     *
     * NOTE: do not rename this method as it is intended to align with the same named export of the "fs" module.
     */
    writeFileSync(path: string, data: string | Buffer<ArrayBuffer>, encoding?: string | null): void;
    writeSync(fd: number, data: string | Buffer, encoding?: string | null): void;
    appendFileSync(_path: string, data: string | Buffer<ArrayBuffer>, encoding?: string | null): void;
    watch(_path: string, ...opts: any[]): {
        close: () => void;
    };
    watchMap: Map<string, Set<[NodeJS.Timeout, WatchFileListener]>>;
    watchFile(path: string, ...opts: any[]): void;
    unwatchFile(path: string, listener?: WatchFileListener): void;
    openSync(path: string, mode: string): number;
    closeSync(fd: number): void;
    constants: {
        R_OK: string;
        W_OK: string;
    };
    accessSync(path: string, type: string): void;
    appendFile(...a: any[]): Promise<any>;
    access(...a: any[]): Promise<any>;
    close(...a: any[]): Promise<any>;
    cp(...a: any[]): Promise<any>;
    exists(...a: any[]): Promise<any>;
    link(...a: any[]): Promise<any>;
    lstat(...a: any[]): Promise<any>;
    mkdir(...a: any[]): Promise<any>;
    open(...a: any[]): Promise<any>;
    readdir(...a: any[]): Promise<any>;
    readFile(...a: any[]): Promise<any>;
    readlink(...a: any[]): Promise<any>;
    realpath(...a: any[]): Promise<any>;
    rename(...a: any[]): Promise<any>;
    rm(...a: any[]): Promise<any>;
    rmdir(...a: any[]): Promise<any>;
    stat(...a: any[]): Promise<any>;
    symlink(...a: any[]): Promise<any>;
    unlink(...a: any[]): Promise<any>;
    utimes(...a: any[]): Promise<any>;
    writeFile(...a: any[]): Promise<any>;
    write(...a: any[]): Promise<any>;
}
export declare class DeviceManager {
    fs: FileSystem;
    constructor(fs: FileSystem);
    /**
   * Mounts a physical or virtual file system at a location in this virtual file system.
   *
   * @param mountPoint The path in this virtual file system.
   * @param resolver An object used to resolve files in `source`.
   */
    mountSync(mountPoint: string, resolver: FSTypeName, options?: any): IFileSystem;
    mount(mountPoint: string, resolver: FSTypeName, options?: any): Promise<IFileSystem>;
    unmount(mountPoint: string): Promise<boolean>;
    df(): IFileSystem[];
    currentFstab(): Fstab[];
    commitPromise(): Promise<void[]>;
    readFstab(path: string | undefined, defaultFSTab: Fstab[]): any;
    unmountExceptRoot(): Promise<void>;
    wakeLazies(): Promise<void>;
    loadFstab(tab: Fstab[]): Promise<void>;
}
export interface FileSystemOptions {
    time?: number;
    files?: FileSet;
    cwd?: string;
    meta?: Record<string, any>;
}
export type Axis = "ancestors" | "ancestors-or-self" | "self" | "descendants-or-self" | "descendants";
export interface Traversal {
    /** A function called to choose whether to continue to traverse to either ancestors or descendants. */
    traverse?(path: string, stats: Stats): boolean;
    /** A function called to choose whether to accept a path as part of the result. */
    accept?(path: string, stats: Stats): boolean;
}
export interface FileSystemResolver {
    statSync(path: string): {
        mode: number;
        size: number;
    };
    readdirSync(path: string): string[];
    readFileSync(path: string): FileDataBuffer;
    writeFileSync?(path: string, data: string | Buffer, encoding: string | null): void;
}
export interface FileSystemEntries {
    readonly files: readonly string[];
    readonly directories: readonly string[];
}
export interface FileSystemResolverHost {
    useCaseSensitiveFileNames(): boolean;
    getAccessibleFileSystemEntries(path: string): FileSystemEntries;
    directoryExists(path: string): boolean;
    fileExists(path: string): boolean;
    getFileSize(path: string): number;
    readFile(path: string): string | undefined;
    getWorkspaceRoot(): string;
}
export declare class Stats {
    dev: number;
    ino: number;
    mode: number;
    nlink: number;
    uid: number;
    gid: number;
    rdev: number;
    size: number;
    blksize: number;
    blocks: number;
    atimeMs: number;
    mtimeMs: number;
    ctimeMs: number;
    birthtimeMs: number;
    atime: Date;
    mtime: Date;
    ctime: Date;
    birthtime: Date;
    constructor();
    constructor(dev: number, ino: number, mode: number, nlink: number, rdev: number, size: number, blksize: number, blocks: number, atimeMs: number, mtimeMs: number, ctimeMs: number, birthtimeMs: number);
    isFile(): boolean;
    isDirectory(): boolean;
    isSymbolicLink(): boolean;
    isBlockDevice(): boolean;
    isCharacterDevice(): boolean;
    isFIFO(): boolean;
    isSocket(): boolean;
}
/**
 * A template used to populate files, directories, links, etc. in a virtual file system.
 */
export interface FileSet {
    [name: string]: DirectoryLike | FileLike | Link | Symlink | Mount | Rmdir | Unlink | null | undefined;
}
export type DirectoryLike = FileSet | Directory;
export type FileLike = File | Buffer | string;
/** Extended options for a directory in a `FileSet` */
export declare class Directory {
    readonly files: FileSet;
    readonly meta: Record<string, any> | undefined;
    constructor(files: FileSet, { meta }?: {
        meta?: Record<string, any>;
    });
}
/** Extended options for a file in a `FileSet` */
export declare class File {
    readonly data: Buffer | string;
    readonly encoding: string | undefined;
    readonly meta: Record<string, any> | undefined;
    constructor(data: Buffer | string, { meta, encoding }?: {
        encoding?: string;
        meta?: Record<string, any>;
    });
}
export declare class SameFileContentFile extends File {
    constructor(data: Buffer | string, metaAndEncoding?: {
        encoding?: string;
        meta?: Record<string, any>;
    });
}
export declare class SameFileWithModifiedTime extends File {
    constructor(data: Buffer | string, metaAndEncoding?: {
        encoding?: string;
        meta?: Record<string, any>;
    });
}
/** Extended options for a hard link in a `FileSet` */
export declare class Link {
    readonly path: string;
    constructor(path: string);
}
/** Removes a directory in a `FileSet` */
export declare class Rmdir {
    _rmdirBrand?: never;
}
/** Unlinks a file in a `FileSet` */
export declare class Unlink {
    _unlinkBrand?: never;
}
/** Extended options for a symbolic link in a `FileSet` */
export declare class Symlink {
    readonly symlink: string;
    readonly meta: Record<string, any> | undefined;
    constructor(symlink: string, { meta }?: {
        meta?: Record<string, any>;
    });
}
/** Extended options for mounting a virtual copy of an external file system via a `FileSet` */
export declare class Mount {
    readonly source: string;
    readonly resolver: FileSystemResolver;
    readonly meta: Record<string, any> | undefined;
    constructor(source: string, resolver: FileSystemResolver, { meta }?: {
        meta?: Record<string, any>;
    });
}
type FileDataBuffer = {
    encoding?: undefined;
    data: Buffer;
} | {
    encoding: BufferEncoding;
    data: string;
};
export declare class FileSystem_Promises {
    fs: FileSystem;
    constructor(fs: FileSystem);
    appendFile(...args: Parameters<FileSystem["appendFileSync"]>): Promise<void>;
    access(...args: Parameters<FileSystem["accessSync"]>): Promise<void>;
    close(...args: Parameters<FileSystem["closeSync"]>): Promise<void>;
    cp(...args: Parameters<FileSystem["cpSync"]>): Promise<void>;
    exists(...args: Parameters<FileSystem["existsSync"]>): Promise<boolean>;
    link(...args: Parameters<FileSystem["linkSync"]>): Promise<void>;
    lstat(...args: Parameters<FileSystem["lstatSync"]>): Promise<Stats>;
    mkdir(...args: Parameters<FileSystem["mkdirSync"]>): Promise<void>;
    open(...args: Parameters<FileSystem["openSync"]>): Promise<number>;
    readdir(path: string): Promise<BaseName[]>;
    readdir(path: string, opt: {
        withFileTypes: true;
    }): Promise<Dirent[]>;
    readFile(...args: Parameters<FileSystem["readFileSync"]>): Promise<string | Buffer<ArrayBuffer>>;
    readlink(...args: Parameters<FileSystem["readlinkSync"]>): Promise<string>;
    realpath(...args: Parameters<FileSystem["realpathSync"]>): Promise<Canonical>;
    rename(...args: Parameters<FileSystem["renameSync"]>): Promise<void>;
    rm(...args: Parameters<FileSystem["rmSync"]>): Promise<void>;
    rmdir(...args: Parameters<FileSystem["rmdirSync"]>): Promise<void>;
    stat(...args: Parameters<FileSystem["statSync"]>): Promise<Stats>;
    symlink(...args: Parameters<FileSystem["symlinkSync"]>): Promise<void>;
    unlink(...args: Parameters<FileSystem["unlinkSync"]>): Promise<void>;
    utimes(...args: Parameters<FileSystem["utimesSync"]>): Promise<void>;
    writeFile(...args: Parameters<FileSystem["writeFileSync"]>): Promise<void>;
    write(...args: Parameters<FileSystem["writeSync"]>): Promise<void>;
}
export {};
