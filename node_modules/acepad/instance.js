import {events} from './events.js';
import {setConfig,getMainEditor,getCurrentEditor,setCurrentEditor,findWord} from './states.js';
import {attachCommands,detachCommands} from './command.js';
import {unsetSel} from './editmode.js';
import {locate,currentLine,goLineEnd,print} from './cursor.js';
import {sessionInfo,createSession,changeSession,
    findSession,findSessions,removeSession,
    recentInfos,
} from './sessions.js';
import {doClick} from "./keyhandler.js";
import {initKey,textToButton,showGuide,renderModifierState,modifiers} from '@acepad/keypad';
import {edit,selectKeyDevice,input} from "./editorcore.js";

export function getInstance(){
    return {
        editor: getCurrentEditor(),
        events,
        edit,selectKeyDevice,input,
        setConfig,findWord,
        getMainEditor,
        getCurrentEditor,setCurrentEditor,
        unsetSel,attachCommands,detachCommands,
        setModifier:modifiers.setModifier,
        renderModifierState,
        locate,currentLine,print,goLineEnd,
        sessionInfo,createSession,changeSession,
        findSession,findSessions,removeSession,
        recentInfos,
        doClick,
        initKey,textToButton,showGuide,
    };
}
