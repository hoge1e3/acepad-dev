import {getMainEditor} from './states.js';
export function scrollBy(by) {
    const editor=getMainEditor();
    editor.renderer.scrollToLine( editor.renderer.getScrollTopRow()+by);
}
export function getKeypadTopRow() {
    const editor=getMainEditor();
    let top=document.querySelector("#keypad").getBoundingClientRect().top;
    return editor.renderer.pixelToScreenCoordinates(0,top).row;
}
export function scrollToKeypadTop() {
    /* This features are disabled:
    An attempt to expand height of the editor  
    overlapping the bottom of the editor body 
    with the top of the semi-transparent keypad.
    `scrollToKeypadTop` was designed to scroll 
    so the caret wouldn't overlap the keypad,
    but currently it's not working properly.
    */
    return ;
    /*
    const editor=getMainEditor();
    let r=getKeypadTopRow();
    if (!r) return;
    r--;
    let cr=editor.session.getSelection().getRange().start.row;
    //console.log(cr,r);
    if (cr>r) {
        scrollBy(cr-r);
    }*/
}