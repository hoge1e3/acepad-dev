//todo: rename as defaultConfig.js
import {setConfig,getCurrentEditor} from './states.js';
import {quitEdit} from './editmode.js';
import {modifierStateToInt} from './modifier.js';
export function initDefaultConfig(){
    setConfig({
        commands:{
            "ctrl-w":(editor)=>editor.session.foldAll(),
            quitEdit,
            unindent(editor){
                //alert("unindent");
                editor.onCommandKey(
                    {},
                    modifierStateToInt({shift:1}), 
                    9);
            },
        },
        menus:{},
        jshint:{
            maxerr:10000,
            esnext:false,
            esversion:11,
            undef:true,
        },
        editor:{
            tabSize:2,
        },
    });
}

