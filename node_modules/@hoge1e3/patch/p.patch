diff --git a/src/patch.ts b/src/patch.ts
--- a/src/patch.ts
+++ b/src/patch.ts
@@ -54,6 +54,22 @@ export  function apply(originalText:string, hunks:string[][], patchText:string,
       const oldStart = parseInt(m[1]) - 1; // 0-indexed
       const oldCount = m[2] !== undefined ? parseInt(m[2]) : 1;
 
+      // Validate context and removed lines match existing content
+      {
+        let idx = oldStart + offset;
+        for (const bl of body) {
+          if (bl.startsWith(" ")) {
+            const expected = bl.slice(1);
+            if (result[idx] !== expected) throw new Error("Context mismatch");
+            idx++;
+          } else if (bl.startsWith("-")) {
+            const expected = bl.slice(1);
+            if (result[idx] !== expected) throw new Error("Removal mismatch");
+            idx++;
+          }
+        }
+      }
+
       // Build replacement lines from hunk body
       const newLines = [];
       for (const bl of body) {
diff --git a/js/patch.js b/js/patch.js
--- a/js/patch.js
+++ b/js/patch.js
@@ -54,6 +54,22 @@ export function apply(originalText, hunks, patchText, filePath) {
         const oldStart = parseInt(m[1]) - 1; // 0-indexed
         const oldCount = m[2] !== undefined ? parseInt(m[2]) : 1;
+        // Validate context and removed lines match existing content
+        {
+            let idx = oldStart + offset;
+            for (const bl of body) {
+                if (bl.startsWith(" ")) {
+                    const expected = bl.slice(1);
+                    if (result[idx] !== expected)
+                        throw new Error("Context mismatch");
+                    idx++;
+                }
+                else if (bl.startsWith("-")) {
+                    const expected = bl.slice(1);
+                    if (result[idx] !== expected)
+                        throw new Error("Removal mismatch");
+                    idx++;
+                }
+            }
+        }
         // Build replacement lines from hunk body
         const newLines = [];
         for (const bl of body) {