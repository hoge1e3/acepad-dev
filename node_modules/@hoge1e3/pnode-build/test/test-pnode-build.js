import * as b from "@hoge1e3/pnode-build";
import * as assert from "assert";
export async function main(){
  this.cd("test");
  await b.build(
    this.resolve("test.js"),
    this.resolve("built.js"));
}    
