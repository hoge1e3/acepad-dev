import { SFile, FileSystemFactory } from '@hoge1e3/sfile';
export declare class Shell {
    private cwd;
    FS: FileSystemFactory;
    constructor(cwd: string, FS: FileSystemFactory);
    resolve(_path: string): SFile;
    getcwd(): string;
    echo(msg: string): void;
}
