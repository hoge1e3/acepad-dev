import { DirTree } from "./dot-sync";
type Delta = {
    lastUpdate: number;
    deleted?: boolean;
    created?: boolean;
    modified?: boolean;
};
type Deltas = {
    [key: string]: Delta;
};
type DeltaDelta = {
    downloads: {
        [key: string]: Delta;
    };
    conflicts: {
        [key: string]: {
            local: Delta;
            remote: Delta;
        };
    };
    uploads: {
        [key: string]: Delta;
    };
};
export declare function getDelta(before: DirTree, after: DirTree): Deltas;
export declare function getDeltaDelta(local: Deltas, remote: Deltas): DeltaDelta;
export {};
