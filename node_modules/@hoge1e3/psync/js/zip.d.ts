import { SFile, Content, DirectoryOptions } from "@hoge1e3/sfile";
export type ProgressOptions = {
    progress?: (f: SFile) => Promise<any>;
};
export type CreateZipOptions = DirectoryOptions & ProgressOptions;
export type UnzipOptions = {
    onCheckFile?: (f: SFile, c: Content) => unknown;
    overwrite?: boolean;
    v?: boolean;
} & ProgressOptions;
export type Status = {
    file: SFile;
    status: "uploaded" | "canceled";
    redirectedTo?: SFile;
};
export declare class zip {
    static zip(dir: SFile, dstZip: SFile, options: CreateZipOptions): Promise<void>;
    static unzip(source: SFile, destDir: SFile, options?: UnzipOptions): Promise<{
        [key: string]: Status;
    }>;
}
