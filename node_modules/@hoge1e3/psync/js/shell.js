import * as path from 'path';
export class Shell {
    constructor(_cwd, FS) {
        this._cwd = _cwd;
        this.FS = FS;
    }
    resolve(_path) {
        if (path.isAbsolute(_path)) {
            return this.FS.get(_path);
        }
        else {
            return this.FS.get(path.join(this.getcwd(), _path));
        }
    }
    getcwd(){return this._cwd;}
    echo(msg) {
        console.log(msg);
    }
}
//# sourceMappingURL=shell.js.map