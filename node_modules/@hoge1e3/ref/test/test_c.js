#!run 
import {ref,custom,V} from "@hoge1e3/ref";
export function main(){
  const x=ref(3);
  const c=custom(
    ()=>x[V], (_)=>x[V]=_);
  c.on("change",(e)=>
  console.log("c",e));
  x.on("change",(e)=>
  console.log("x",e));
  this.echo(c.value);
  c[V]=50;
  this.echo(c.value);
  this.echo(x.value);
  
}