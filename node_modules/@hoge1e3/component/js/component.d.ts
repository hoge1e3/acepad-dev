import { ReadonlyRef, RefValues } from "@hoge1e3/ref";
export declare function c<RS extends readonly ReadonlyRef<unknown>[]>(...args: [
    any,
    ...res: RS,
    (...values: RefValues<RS>) => HTMLElement
]): HTMLElement;
