import * as splashscreen from "@hoge1e3/splashscreen";
import * as assert from "assert";
export async function main(){
    splashscreen.show("test");
    await this.sleep(1);
    splashscreen.hide();
    await splashscreen.wrap(this.sleep(1));
    await splashscreen.wrap(this.sleep(1),"foo");
    await splashscreen.wrap(bar)(this,"bar");
}    
async function bar(t,x){
    for(let i=0;i<10;i++){
    await t.sleep(0.1);
    splashscreen.show(x+i);
    }
    
}
