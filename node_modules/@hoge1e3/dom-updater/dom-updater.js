// @hoge1e3/dom-updater
export function update(dst,src,{
    findUpdatable,
    isUpdatable,
    idSkippable,
}){
    const updator={
        trashed:new Set(),
        kept:new Set(),
        keep(dom){
            
        },
    };
    function u(d, s){
        uattr(d, s);
    }
    function uattr(d, s){
        for(let a of s){
            
        }
    }
    u(dst, src);
}