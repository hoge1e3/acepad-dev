// @hoge1e3/dom-updater
export function update(dst,src,{
    findUpdatable,
    isUpdatable,
    shouldSkip
}){
    const updator={
        trashed:new Set(),
        trash(dom){
            
        },
        untrash,
        used,
    };
    
}