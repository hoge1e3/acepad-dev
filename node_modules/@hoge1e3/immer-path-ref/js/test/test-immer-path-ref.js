#!runts
import { fromRef } from "../immer-path-ref.js";
//import {create as cp,PathRefProxy} from "../proxy.js";
import { ref, change } from "@hoge1e3/ref";
export async function main() {
    const root = {
        title: "test",
        sub: [
            { title: "sub1", sub: [] },
            { title: "sub2",
                sub: [
                    { title: "subsub", sub: [] }
                ],
            },
        ],
    };
    const rr = ref(root);
    const rootRef = fromRef(rr);
    change(rootRef, (e) => {
        console.log("r", e);
    });
    console.log("v0", rootRef.value);
    //return;
    const r1 = rootRef.join("title");
    r1.value = "TITLE!";
    console.log("v1", rootRef.value);
    console.log("v1", rr.value);
    const r1s = rootRef.join("sub"); // as any as PathRef<Item[]>;
    const r2 = r1s.join(1);
    r2.value = { title: "sub3", sub: [] };
    console.log("v3", rootRef.value);
    //const r3=rootRef("sub")(0)
}
//# sourceMappingURL=test-immer-path-ref.js.map