#!runts
import { create } from "../immer-path-ref.js";
import { ref } from "@hoge1e3/ref";
export async function main() {
    const root = {
        title: "test",
        sub: [
            { title: "sub1" },
            { title: "sub2",
                sub: [
                    { title: "subsub" }
                ],
            },
        ],
    };
    const rr = ref(root);
    const rootRef = create(rr);
    rootRef.on("change", (e) => {
        console.log("r", e);
    });
    console.log("v0", rootRef.value);
    //return;
    const r1 = rootRef.join("title");
    r1.value = "TITLE!";
    console.log("v1", rootRef.value);
    console.log("v1", rr.value);
    const r1s = rootRef.join("sub");
    const r2 = r1s.join(1);
    r2.value = { title: "sub3" };
    console.log("v3", rootRef.value);
}
//# sourceMappingURL=test-immer-path-ref.js.map