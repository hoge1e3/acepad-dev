import {id} from "@hoge1e3/dom-id";
import * as assert from "assert";
export async function main(){
  const i=id();
  this.echo(i);
  this.echo(i.test);
  for(let k=0;k<10;k++){
  this.echo(i());
  }
}    
