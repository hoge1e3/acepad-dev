import {css,TreeView} from "@hoge1e3/treeview";
import * as assert from "assert";
import {showWidget} from "acepad-widget";

export async function main(){
    css();
    const w=showWidget("<div>",{
        width:30,
    })  ;  
    new TreeView([{
        name:"hoge",children:   [{
            name:"fuga",
        }
            
        ]
    }],w.element);
    $(w.element).on("touchstart",(e)=>{
        e.stopPropagation();
        //e.preventDefault();
    });
}    
