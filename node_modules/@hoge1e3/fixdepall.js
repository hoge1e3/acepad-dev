#!run

export async function main(){
    for (let f of this.resolve(".").listFiles()) {
        if (!f.isDir()) continue;
        if (!f.rel("package.json").exists()) continue;
        this.echo(f);
        this.cd(f);
        await this["npm-fixdeps"]();
        this.cd("../");
    }
}
