export class KeySet<T> {
    constructor(key: (item: T) => string);
    add(e: T): void;
    delete(e: T): void;
    has(e: T): boolean;
    [Symbol.iterator](): IterableIterator<T>;
    chkuniq(): void;
}