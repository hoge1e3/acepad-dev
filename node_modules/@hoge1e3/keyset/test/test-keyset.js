import * as keyset from "@hoge1e3/keyset";
import * as assert from "assert";
export async function main(){
    const s=new keyset.KeySet(e=>e.id);
    s.add({id:1});
    s.add({id:2});
    s.add({id:2});
    this.echo(...[...s].map(e=>e.id));
    this.echo(s.has({id:1}));
    this.echo(s.has({id:3}));
    s.delete({id:1});
    this.echo(...[...s].map(e=>e.id));
    
}    
