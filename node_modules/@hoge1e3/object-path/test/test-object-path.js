import * as objectpath from "@hoge1e3/object-path";
import * as assert from "assert";
export async function main(){
  assert.ok(objectpath.find(objectpath,"find"));
  assert.ok(!objectpath.find(objectpath,"found"));
  assert.equal(objectpath.find(assert,"ok"),assert.ok);
  assert.equal(objectpath.find(assert,"equal"),assert.equal);
  assert.equal(objectpath.find(globalThis,"console.log"),console.log);
  assert.ok(!objectpath.find(globalThis,"console.lag.error"));
  
}    
