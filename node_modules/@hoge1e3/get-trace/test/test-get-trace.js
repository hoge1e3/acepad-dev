import {getTrace} from "@hoge1e3/get-trace";
import * as assert from "assert";

function test(u,ln,trc){
    trc=trc.shift();
    console.log("gtr",trc);
    assert.ok(trc.includes(u),"should have "+u);
    assert.ok(trc.includes(ln+""),"should have "+ln);
}
export async function main(){
    let d=dir(this);
    
    test(d,14,getTrace());
    
    test(d,16,getTrace());
    
}    
function dir(sh){
    const u=import.meta.url+"";
    return(u);
}
