#!runts
import { ref } from "@hoge1e3/ref";
import { refInput } from "@hoge1e3/ref-input";
//import {PathRefProxy,ReadonlyPathRefProxy,fromVal,map/*PathRef, ReadonlyPathRef*/} from "@hoge1e3/immer-path-ref/js/proxy.js";
export async function main() {
    return this.widget({
        wmain,
        css: "./style.css",
        fullscreen: 1
    }) && undefined;
}
function wmain({ id: wid, t, on }) {
    //this.echo("key");
    const srcRef = ref("");
    return t.div({ class: "src" }, refInput(srcRef, t.textarea({
        id: wid.src,
        class: "src",
    })));
}
function parseSrc(s) {
    /*
    Each section consists of:
    #[gakuseki/id]: .*
    ##[attribute_name1].*
    (some content, it may be multiple lines)
    ##[attribute_name2].*
    (some content, it may be multiple lines)
    ##[attribute_name2].*
    (if there are same names, these should be string array)
    
    */
}
//# sourceMappingURL=marker.js.map