#!run
import {tarDecode} from "@hoge1e3/tar";
export async function main(){
  const r=await fetch("https://registry.npmjs.org/petit-node/-/petit-node-1.6.9.tgz");
  console.log(r);
  const j=new Uint8Array(await r.arrayBuffer());
  //console.log(j);
  for (let f of await tarDecode(j,true)){
    this.echo(f.size, f.name);    
  }
  return j.length;
}
