//import {init} from "@acepad/mode-shell/acepad-mode-shell2.js";
import {init} from "@acepad/mode-shell";
import * as assert from "assert";
import {sessionInfo} from "@acepad/sessions";
export async function main(){
  init();
  const editor=this.$acepad.getMainEditor()
  sessionInfo(editor.session,
  {sh:this});
  editor.session.setMode(
    "ace/mode/shell")
}    
/*
sh: editcmd 
*/