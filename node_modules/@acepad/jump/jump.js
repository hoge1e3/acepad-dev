// @acepad/jump
export function jump(acepad){
    const editor=acepad.getCurrentEditor();
    const session=editor.session;
    let r=session.getSelection().
    getRange().start.row;
    let line=session.doc.getLine(r);

}