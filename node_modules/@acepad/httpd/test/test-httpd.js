import * as httpd from "@acepad/httpd";
import * as assert from "assert";
export async function main(){
   const url=await httpd.serve("test",
   (req,res)=>{
    res.writeHead(200, { 
      "Content-Type": "text/html;charset=utf8" });
     res.end(`test<br>
    <a href="javascript:location.reload()">
    reload</a>
     ${new Date()}
     `);
   })   ;
   console.log(url);
   await this.sleep(0.1);
   this.webpage(url);
}    
