// @acepad/retry
import {wrap} from "@acepad/splashscreen";

export async function retry(p){
  while(true){
    try{
      return await p();
    }catch(e){
      if(e.retryPromise){
        await wrap(e.retryPromise);
        //return p();
      }else{
        throw e;
      }
    }
  }
}
