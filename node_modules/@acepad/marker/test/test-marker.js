import * as marker from "@acepad/marker";
import * as assert from "assert";
export async function main(){
  const session=this.$acepad.getMainEditor().session;
  const index=10;
  const length=5;
  let p=session.doc.indexToPosition(index);
  let e=session.doc.indexToPosition(index+length);
  let m=marker.add(
    session, 
    ace.Range.fromPoints(p, e), {
   
  });
  console.log(m)
}    
