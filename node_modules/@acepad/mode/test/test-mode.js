import * as mode from "@acepad/mode";
import * as assert from "assert";
export async function main(){
    

mode.createMode("alert", {
  highlights: [
    { regex: /TODO:/, css: "color:red; font-weight:bold;" },
    { regex: /FIXME:/, css: "color:orange;" }
  ],
// FIXME:
  getCompletions(editor, session, pos, prefix) {
    return Promise.resolve([
      {
        caption: "TODO:",
        value: "TODO:",
        meta: "keyword"
      },
      {
        caption: "FIXME:",
        value: "FIXME:",
        meta: "keyword"
      }
    ]);
  }
});
const editor=this.$acepad.
getMainEditor();
editor.session.setMode("ace/mode/alert");
}