import * as keypad from "@acepad/keypad";
import * as assert from "assert";
export async function main(){
 this.echo(`export {${Object.keys(keypad)}} from "@acepad/keypad";`);
 //.renderModifierState+"")   
}    
