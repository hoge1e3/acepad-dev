#!run
// @acepad/mcp

import {t} from "@hoge1e3/dom";
import {show} from "@acepad/widget";

function d2j(d){
  const r={};
  for(let f of d.recursive({excludes:["test/"]})){
    if(f.endsWith(".js.map")) continue;
    r[f.relPath(d)] = f.ext()===".json" ? f.obj() : f.text();    
  }
  return r;
}

export async function main(){
  const sh=this;
  const d=sh.cwd();
  
  const w=show({overlay:true});

  let userPrompt="";

  function buildPrompt(){
    return `
${userPrompt}

You are generating a machine-readable changed files.
You must answer in pure JSON.
Do not output explanation.

Rules:
- Output exactly one JSON object.
- The key is the file path.
- The value is the base64-encoded string (Decodable by Uint8Array.fromBase64) of the file content.
- Do not use markdown.

Format:
{
  "path/to/file.js": "aW1wb3J0ICogYXMgZnMgZnJvbSAiZnMiOw=="
}

~~~json
${JSON.stringify(d2j(d))}
~~~
  `;
    }
  
    const a=(f,c)=>t.a({href:"javascript:;",onclick:f},c);
  
    w.print(
      t.div({style:"height:100%; overflow: scroll;"},
        t.div("Custom Prompt"),
        //prompt: fit textarea size into widget size
        t.div(t.textarea({
          rows: 8,
          cols: 100,
          oninput(e){
            userPrompt=e.target.value;
          }
        },userPrompt)),
        t.ol(
          t.li(a(cp,"Copy prompt")),
          t.li(
            t.a({
              target:"ai", 
              href:process.env.genai||"https://chatgpt.com/"
            },"Open GenAI"),
            " and paste prompt."
          ),
          t.li(a(ps,"Paste result")),
        ),
      )
    );
  
    function cp(){
      const pr=buildPrompt();
      navigator.clipboard.writeText(pr);
    }
  
    async function ps(){
      const s=await navigator.clipboard.readText();
      let o;
      try{
        o=JSON.parse(s);
      }catch(e){
        alert("Invalid JSON");
        return;
      }
    
      const keys=Object.keys(o);
      let k,f;
    
      const str=(s)=>typeof s==="string"?s:JSON.stringify(s,null,4);
    
      function showResult(_k){
        k=_k;
        f=k && d.rel(k);
      
        w.print(
          t.div({style:"height:100%; overflow: scroll;"},
            t.div("Result"),
            ...keys.map(k=>
              t.div(a(()=>{showResult(k)},k))
            ),
            ...(f ? [
              t.div(t.button({
                onclick(){
                  f.setBytes(Uint8Array.fromBase64(str(o[k])));
                  keys.splice(keys.indexOf(k),1);
                  showResult();
                },
              },`Accept ${k}`)),
              t.div(t.textarea({
                rows: 20,
                cols: 100,
              },str(o[k])))
            ] : []),
          )
        );
      }
    
      showResult();
    }
  }