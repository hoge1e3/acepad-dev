diff --git a/filetree.css b/filetree.css
--- a/filetree.css
+++ b/filetree.css
@@ -8,6 +8,12 @@
 .filetree-root .item:active{
     background-color: #aaf;
 }
+.filetree-root .item.flash{
+    opacity: 1 !important;
+    background-color: #aaf;
+    transition: none;
+}
+
 .filetree-root .item{
     position: sticky;
   top: calc(var(--level, 0) * 20px); 
diff --git a/render.js b/render.js
--- a/render.js
+++ b/render.js
@@ -37,6 +37,13 @@ export function render(home,onsel){
     function onclick(e){
         const target=e.target;
         if(target.classList.contains("item")){
+            target.classList.add("flash");
+            target.style.transition = "none";
+            requestAnimationFrame(()=>{
+                requestAnimationFrame(()=>{
+                    target.style.transition = "opacity 0.4s";
+                    target.style.opacity = "0.5";
+                    setTimeout(()=>{ target.style.transition=""; target.style.opacity=""; target.classList.remove("flash"); }, 400);
+                });
+            });
             const path=target.getAttribute("data-path");
             const f=file(path);