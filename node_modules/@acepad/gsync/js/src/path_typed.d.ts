import { FilePath } from "./types.js";
export declare function join(p: FilePath, ...rest: string[]): FilePath;
export declare function normalize(p: FilePath): FilePath;
export declare function dirname(p: FilePath): FilePath;
