import * as flex from "@acepad/flex";
import * as assert from "assert";
import {createProxy} from "@acepad/worker";
import {search} from "../flex-worker-client.js";

export async function main(){
  for await (let r of search("resolve",this.resolve(this.$home))){
    console.log("r",r);
  }
}
export async function main2(){
    const p=await createProxy(this.resolve("./flex-worker.js"),{
      send(...a){
        console.log("sent",a);
      }
    });
    p.initDb(this.$home);
    const r=await p.main("resolve",this.$home);
    console.log("res",r);
}    
