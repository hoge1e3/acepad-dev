import * as here from "@acepad/here";
import * as assert from "assert";
export async function main(){
    assert.ok(
        here.sibling(import.meta.url,"test-here.js").
        text().includes("hogefuga")
    );
}    
