let root;
export function getRoot(f) {
  if(root)return root;
    while(f){
      const p=f.up();
      if(!p){root=f;return f;}
      f=p;
    }
}
export function getMetaRoot(f){
    return getRoot(f).rel(".meta/");
}