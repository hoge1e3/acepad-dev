import bottomup from "@acepad/bottomup";
import * as assert from "assert";
export async function main(){
    let i=0;
    for(let f of bottomup(this.resolve(__dirname))){
        if(i%10==0)this.echo(f);
        if(i++>200)break;
    }
}    
