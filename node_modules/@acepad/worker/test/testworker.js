import * as fs from "fs";
export function test(x){
    console.log("from worker x=",x);
    this.back(300);
    return x*5;
}
export function writeLog(path) {
    fs.appendFileSync(path, new Date()+"\n");
}