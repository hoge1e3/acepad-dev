import {canvas} from "@acepad/canvas";
export async function main(){
  const {w,h,ctx,raf}=canvas();
  for(let i=0;i<h;i++){
    ctx.fillRect(
      (i+(i>h/2?1:0))%2*w/2,
      i,w/2,1);
    await raf();
  }
}    
