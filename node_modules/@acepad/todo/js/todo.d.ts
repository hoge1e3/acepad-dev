export interface Item {
    id: string;
    title: string;
    ok?: boolean;
    sub: Item[];
}
export declare function decode(text: string): Item[];
export declare function encode(data: Item[]): string;
