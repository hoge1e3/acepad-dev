import { Ref } from "@hoge1e3/ref";
type RefList<T> = ReadonlyArray<Ref<T>>;
export interface Item {
    id: string;
    title: string;
    ok?: boolean;
    sub: Ref<RefList<Item>>;
}
export interface ItemDraft {
    id: string;
    title: string;
    ok?: boolean;
    sub: ItemDraft[];
}
export declare function decode(text: string): RefList<Item>;
export declare function encode(data: RefList<Item>): string;
export {};
