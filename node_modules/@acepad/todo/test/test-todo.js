import * as todo from "@acepad/todo";
import * as assert from "assert";
export async function main(){
    const d=todo.decode(this.resolve("/idb/run/todo.txt").text())
    const e=todo.encode(d);
    return e;
}    
