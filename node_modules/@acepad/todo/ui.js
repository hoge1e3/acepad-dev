#!run
import * as kstr from "@hoge1e3/key-stream";
import {ref} from "@hoge1e3/ref";
import {decode} from "./todo.js";
export async function main(){
  return this.widget(wmain);
}    
function wmain({id:idgen,t}){
  //this.echo("key");
  
  kstr.addEventListener("down",
  (e)=>this.echo(e.key))
}