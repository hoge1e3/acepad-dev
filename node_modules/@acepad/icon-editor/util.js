export function drawGrid(ctx, size, scale) {
  ctx.save();
  ctx.beginPath();
  for (let i=0;i<=size;i++) {
    ctx.moveTo(0.5 + i*scale, 0);
    ctx.lineTo(0.5 + i*scale, size*scale);
    ctx.moveTo(0, 0.5 + i*scale);
    ctx.lineTo(size*scale, 0.5 + i*scale);
  }
  ctx.strokeStyle = 'rgba(0,0,0,.25)';
  ctx.lineWidth = 1;
  ctx.stroke();
  ctx.restore();
}
export function rgbaToHex(rgba) {
  if (rgba==null) return null;
  // Expect rgba(r,g,b,a)
  const m = rgba.match(/rgba?\((\d+),(\d+),(\d+)/);
  if (!m) return null;
  const r = (+m[1]).toString(16).padStart(2,'0');
  const g = (+m[2]).toString(16).padStart(2,'0');
  const b = (+m[3]).toString(16).padStart(2,'0');
  return `#${r}${g}${b}`;
}
export function clamp(v, min, max) { return Math.max(min, Math.min(max, v)); }
export function drawChecker(ctx, w, h, cell = 8) {
  ctx.save();
  for (let y=0; y<h; y+=cell) {
    for (let x=0; x<w; x+=cell) {
      const odd = ((x/cell) + (y/cell)) & 1;
      ctx.fillStyle = odd ? '#9aa0a6' : '#cfd3d6';
      ctx.fillRect(x,y,cell,cell);
    }
  }
  ctx.restore();
}