#!p5
let x=0;
export function setup(){
  createCanvas(windowWidth, windowHeight);
  rect(0,0,25,30);
}
export function draw(){
  background("cyan");
  rect(x++,mouseY,25,30);
  if(x>=windowWidth)x=0;
}