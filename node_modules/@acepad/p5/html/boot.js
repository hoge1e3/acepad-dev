import {loadScriptTag} from "@hoge1e3/loadScript";
import * as pNode from "petit-node";
parent.console.log("config",pNode)
async function boot(p5){
  const m=await pNode.importModule(p5);
  let x=0;
  globalThis.setup=m.setup;
  globalThis.draw=m.draw;
  /*()=>{
    createCanvas(windowWidth, windowHeight);

    rect(0,0,25,30);
  }
  globalThis.draw=()=>{
    rect(x++,mouseY,25,30);
    
  }*/
  await loadScriptTag("https://cdn.jsdelivr.net/npm/p5@1.11.13/lib/p5.min.js");
}
globalThis.boot=boot;