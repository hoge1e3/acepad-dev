#!run
import {t} from "@hoge1e3/dom";

import {did} from "@hoge1e3/dom-id";
import {show} from "./acepad-widget.js";
export function main(dom){
  const id=did("w");
  const rdom=t.div({id});
  const w=show(rdom,{overlay:true});
  Object.assign(w,{t,id});
  if(typeof dom==="function"){
    dom=dom(w);
  }
  if(dom){
    w.print(dom);
  }
  return w;
}