//import * as file-icon from "@acepad/file-icon";
import {show,showIframe} from "@acepad/widget";
import {open} from "@acepad/browser";
import {sibling} from "@acepad/here";
export async function main(){
    //const w=show(); 
    const html=sibling(import.meta.url,"../file-icon.html");
    //w.element.innerHTML=`<div class="head"></div><div class="body"></div>`;
    
    showIframe(html,{
      pNode:true,  
      fullscreen:1
    });/*, {
        document: {
            querySelector(q){
              q="."+q;
                const res=w.element.querySelector(q);
                if (!res) {
                  console.log(w.element);
                  throw new Error(`${q} not found`);
                }
                return res;
            },
            createTextNode(...a){
              return document.createTextNode(...a);
            },
            createElement(...a){
              return document.createElement(...a);
            },
            
        },    
    });*/

}    
